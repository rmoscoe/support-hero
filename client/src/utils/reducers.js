import {
    GET_TICKET,
    GET_USER_TICKETS,
    CREATE_TICKET,
    UPDATE_TICKET_STATUS,
    CREATE_COMMENT,
    CREATE_NOTE,
    UPDATE_NOTE,
    DELETE_NOTE,
    LOGIN,
    CREATE_USER
} from "./actions";

export default function reducer(state, action) {
    switch (action.type) {
        case GET_TICKET: {
            
        }
    }
}